/**
 * Page-hero band height, including the strip under the fixed site header.
 * Pattern pitch and edge crops are derived from the active value.
 * Compact is the phone width, below the same 768px split as the site header.
 */
export const HERO_HEIGHT = 320;
export const HERO_HEIGHT_COMPACT = 240;
export const HERO_COMPACT_QUERY = "(max-width: 767px)";
